name: First Workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v2
        
      - name: "Print greeting"
      - run: echo "Hello world"
      
      - name: "Print goodbye"
      - run: echo "Goodbye"
